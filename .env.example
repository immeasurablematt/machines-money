# Copy this file to .env for local runs and fill in values there. Never put
# real keys in this file: it is committed to a public repository.
# For GitHub Actions, set these as repository secrets instead
# (Settings > Secrets and variables > Actions).
X_BEARER_TOKEN=
DUNE_API_KEY=
COINGECKO_DEMO_API_KEY=
